# DCCV Web Client Application

How to run with gradle:

```
gradle runReact
```

How to build with Docker:
```
docker build -t react-client-image .
```

How to run with Docker:
```
docker run --name react-client-container -it --rm react-client-image
```